# SECURITY_REVIEW.md — Аудит безопасности (TASK-13)

## 1. Защита маршрутов и CORS
- API-эндпоинты (`/api/*`) защищены Bearer-токенами и корректными заголовками `Access-Control-Allow-Origin: *`.
- Поддомен `auth.*` изолирован и не передает токены третьим сторонам.

## 2. Вердикт
`APPROVED`
