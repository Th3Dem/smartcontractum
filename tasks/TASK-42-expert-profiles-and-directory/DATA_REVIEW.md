# DATA_REVIEW.md — Аудит модели данных (TASK-42)

## 1. Проверка
- Данные экспертов агрегируются из `users`, `user_reputation`, `feed_posts` и `feed_comments`.
- Чувствительные персональные данные (хэши паролей, соли, паспортные номера) исключаются через `sanitize_user_dict()` в соответствии с 152-ФЗ РФ.
- Публичный доступ предоставляется только к открытым полям: ФИО, название компании, блог, компетенции, очки репутации и публичные статьи.

## 2. Вердикт
`APPROVED`
