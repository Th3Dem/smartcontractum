# DATA_REVIEW.md — Аудит базы данных (TASK-10)

## 1. Схема данных и хеширование
- Таблица: `users`
- Колонки: `password_hash` (TEXT), `password_salt` (TEXT)
- Алгоритм: PBKDF2-HMAC-SHA256, 100 000 итераций, 16-байтная соль (128 бит случайной энтропии).
- Проверка: При каждом обновлении пароля соль регенерируется через `secrets.token_hex(16)`, обеспечивая защиту от радужных таблиц.

## 2. Вердикт
`APPROVED`
