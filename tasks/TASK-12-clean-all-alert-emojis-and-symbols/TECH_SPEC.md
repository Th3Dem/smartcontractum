# TECH_SPEC.md — Техническая спецификация очистки эмодзи (TASK-12)

## 1. Изменения в коде фронтенда

### 1.1 `public/app.js`
- Строка 1235: `showAlert('Поздравляем! Ваш E-mail подтвержден, аккаунт сохранен в базе данных. Перенаправление в личный кабинет...', 'success');`
- Строка 1488: `showAlert('Пароль успешно изменен! Введите новый пароль для входа в личный кабинет.', 'success');`
- Регулярное выражение очистки:
```javascript
function formatAlertText(message) {
  if (!message) return '';
  return String(message).replace(/^[\s✓✅✔️🎉⚠️🔐🔒🔔💡📌*—–-]+/, '').trim();
}
```
