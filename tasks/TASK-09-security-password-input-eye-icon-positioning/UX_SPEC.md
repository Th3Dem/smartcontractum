# UX_SPEC.md — Интерфейсная спецификация (TASK-09)

## 1. Компоновка поля пароля
- **Высота строки**: 44px.
- **Внутренний отступ текста**: `11px 40px 11px 13px`.
- **Иконка**: Расположена точно по центру вертикали строки (`align-items: center`), с отступом `10px` от правого края.
- **Курсор**: `pointer` при наведении, плавный переход цвета `color: var(--text-primary)` при `:hover`.
- **Состояния**:
  - Пароль скрыт: `👁` (символы маскируются точками `●●●●●●●●`).
  - Пароль открыт: `🙈` (символы видны в виде открытого текста).

## 2. Вердикт
`APPROVED`
