# DATA_REVIEW.md — Аудит данных (TASK-16)

## 1. Структура данных публикаций
- Сохранение клиентских постов, комментариев и голосов поддерживается через `localStorage` и REST API.
- Данные валидируются по типам (`article`, `post`, `question`, `poll`, `rfc`).

## 2. Вердикт
`APPROVED`
