# DATA_REVIEW.md — Аудит модели данных публикаций (TASK-36)

## 1. Проверка
- Данные статьи сохраняются в таблицу `feed_posts` SQLite.
- Валидация обязательных полей (`title` не пустой, `category` входит в допустимый список, `type` валидирован).
- Черновики сохраняются изолированно в `localStorage` браузера пользователя.

## 2. Вердикт
`APPROVED`
